# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# You can and probably must change those credentials in production. However, for development, you can leave them as they are.
# If you change them, don't forget to update the connection DATABASE_URL.
POSTGRES_USER="user"
POSTGRES_PASSWORD="password"

# Prisma Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/postgres"

# NextAuth Configuration 
# Here try generate one with this command "openssl rand -base64 32"
NEXTAUTH_SECRET=""

# Google OAuth Configuration
# Here you need to create an application in your google console and get the associated ID and Secret
GOOGLE_ID=""
GOOGLE_SECRET=""

# You can add other providers, have a look here : https://next-auth.js.org/providers/